export type UrlColumn = {
  name: string;
  url: string;
};

export type DetailColumn = {
  summary: string;
  detail: string;
};

export type ContactHistoryColumn = {
  date: string;
  note: string;
};

export type QuoteColumn = {
  person: string;
  quote: string;
};

export type TemplateColumn = {
  name: string;
  template: string;
};
